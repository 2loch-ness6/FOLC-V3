#!/bin/sh
# This script does NOTHING.
# It is used to mask the vendor UI binaries (qt_daemon)
# so the Watchdog thinks they are running, but they don't touch the screen.

while true; do
    sleep 3600
done
